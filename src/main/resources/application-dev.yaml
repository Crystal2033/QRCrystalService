server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
  datasource:
    url:
      jdbc:postgresql://localhost:5432/qrcompany
    username: postgres
    password: qwerty12345
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update

hostname: localhost:8080